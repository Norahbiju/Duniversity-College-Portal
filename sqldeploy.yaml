apiVersion: apps/v1
kind: Deployment
metadata:
  name: sql-deploy 
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailabe: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: sql-app
  template:
    metadata:
      labels:
        app: sql-app
    spec:
      containers:
        - name: sql-cont 
          image: mysql:8.0
          ports:
            - containerPort: 3306
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          volumeMounts:
            - name: sql-volume 
              path: /var/lib/mysql
      volums:
        - name: sql-volume 
          persistentVolumeClaim:
            claimName: duni-pvc
---
apiVersion: v1
kind: Service
metadata: 
  name: sql-service 
spec:
  type: ClusterIP
  selector:
    app: sql-app
  ports:
    - port: 3306
      targetPort: 3306
